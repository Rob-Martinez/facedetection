import React from "react";

const Logo = () => {
    return (
        <div className="ma4 mt0">
            {/* no logo here, not using this. But will keep in case of need in later use. */}
        </div>
    );
}

export default Logo;